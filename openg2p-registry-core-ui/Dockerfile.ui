# temp-docker-repo/openg2p-registry-core-ui/Dockerfile.ui
FROM node:20-alpine AS base
WORKDIR /app

FROM base AS builder
RUN apk add --no-cache git

ARG REPO_URL=https://github.com/Q-Niranjan/openg2p-registry-core-ui.git
ARG GIT_BRANCH=develop 

RUN git clone --branch ${GIT_BRANCH} --depth 1 ${REPO_URL} .

RUN npm ci

ENV NEXT_TELEMETRY_DISABLED=1
RUN npm run build

FROM base AS runner
ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME=0.0.0.0
ENV NEXT_TELEMETRY_DISABLED=1

RUN addgroup --system --gid 1001 nodejs \
 && adduser --system --uid 1001 nextjs

# Copy built app
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

# Copy core locales from builder
COPY --from=builder --chown=nextjs:nodejs /app/locales ./locales

RUN npm install sharp

# Create directory for MERGED extension locales
RUN mkdir -p /app/extension-locales && \
    chown -R nextjs:nodejs /app/extension-locales

# Simple entrypoint - just copy files, NO merge!
COPY --chown=nextjs:nodejs docker-entrypoint.sh /usr/local/bin/
RUN chmod +x /usr/local/bin/docker-entrypoint.sh

USER nextjs

EXPOSE 3000

ENTRYPOINT ["/usr/local/bin/docker-entrypoint.sh"]
CMD ["node", "server.js"]
